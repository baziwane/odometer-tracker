# Supabase Configuration
# Get these values from your Supabase project settings at https://supabase.com/dashboard/project/_/settings/api

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anonymous key (safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Your Supabase service role key (KEEP SECRET - only use server-side)
# This key bypasses Row Level Security policies
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Security Configuration
# Environment (production enables stricter security measures)
NODE_ENV=development

# Rate Limiting
# Enable/disable rate limiting (true in production, can be false in development)
RATE_LIMIT_ENABLED=true

# Auth endpoints: 5 requests per minute
RATE_LIMIT_AUTH_MAX=5
RATE_LIMIT_AUTH_INTERVAL=60000

# Data endpoints: 100 requests per minute
RATE_LIMIT_API_MAX=100
RATE_LIMIT_API_INTERVAL=60000

# CSRF Protection
# Enable/disable CSRF protection (true in production)
CSRF_ENABLED=true
